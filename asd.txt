import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(exim());
}

class exim extends StatelessWidget {
  const exim({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Directionality(textDirection: TextDirection.rtl, child: s1()),
    );
  }
}

class s1 extends StatelessWidget {
  const s1({super.key});
